"use client";

import React from "react";

const ShakaPlayer = () => {

  return (
    <div
      // ref={containerRef}
      className="aspect-video bg-amber-950 w-full"
      style={{ maxWidth: "1200px" }}
    >
      {/* <video ref={videoRef}></video> */}
    </div>
  );
};

export default ShakaPlayer;
